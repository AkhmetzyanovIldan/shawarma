{
  "name": "kruti-miniapp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "dev": "node --watch server/index.mjs",
    "start": "node server/index.mjs",
    "build": "node --check server/index.mjs && node --check server/domain.mjs && node --check public/app.js",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-check.cjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
